eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
server:
  port: 8002
spring:
  application:
    name: order
    stream:
      bindings:
        myMessage:
          group: order
          content-type: application/json
